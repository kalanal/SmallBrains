<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gethelp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Kalana's Activities -->
        <activity android:name=".SignUp"/>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserProfile"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".ProfessionalDetails"/>
      
        <!-- Sudaraka's Activities -->

        <activity android:name=".ProfessionalProfile"/>
        <activity android:name=".ProblemListPage"/>
        <activity android:name=".CustomerRequestSolution"/>
        <activity
            android:name=".ServiceRequest"
            android:label="@string/service_request"/>
        <activity
            android:name=".CustomerRequest"
            android:label="@string/customer_request"/>
        <activity
            android:name=".ServiceRequestList"
            android:label="@string/service_request_list">
        </activity>
        <activity
            android:name=".PerformanceReport"
            android:label="@string/performance_report"/>

        <!-- Ryans Activities -->
        <activity
            android:name=".Admin.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
<!--             <intent-filter>-->
<!--             <action android:name="android.intent.action.MAIN" />-->


<!--             <category android:name="android.intent.category.LAUNCHER" />-->
<!--             </intent-filter>-->
        </activity>
        <activity android:name=".Consumer.ConsumerMainActivity"/>
    </application>

</manifest>
